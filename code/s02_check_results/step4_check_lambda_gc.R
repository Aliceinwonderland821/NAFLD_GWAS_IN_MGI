# This script compute lambda GC for each GWASes in the input folder
# run as `Rscript step4_check_lambda_gc.R --indir input_dir --infileRegExpr regexprssion --outdir output_directory --dbtype refGene --buildver hg38 --humandb humandb_folder`

